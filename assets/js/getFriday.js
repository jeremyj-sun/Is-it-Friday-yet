
//getFriday(today) consumes the current date and returns the next Friday as a date object.
export function getFriday(today) {
    //placeholder logic
    let dateFuture = new Date(2020, 7, 15, 21, 15, 12);
    return dateFuture;
}
